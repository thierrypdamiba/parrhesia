# Optional: HMAC secret for the identity cookie fallback (see docs/PLAN.md 4.4). Unset locally.
DOCKET_SESSION_SECRET=
# Local only: enables /dev/signin?name=Maya to simulate a signed-in signer.
DEV_IDENTITY=1
